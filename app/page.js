export default function Home() {
  return (
    <main style={{maxWidth: 800, margin: "80px auto", fontFamily: "system-ui"}}>
      <h1 style={{fontSize: 36, marginBottom: 12}}>Avali Creator Platform — MVP</h1>
      <p>Deployed on AWS Amplify (drag & drop ZIP, no Git provider).</p>
      <p>Next step: connect API Gateway + Lambda for backend features.</p>
    </main>
  );
}
